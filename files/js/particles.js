class Particle {

}

function particle(options) {
    if (!options.position)
        console.error("The function particle() options must contain a position");
    else if (!options.type)
        console.error("The function particle() options must contain a type");

        if (options.type === "rect")
        {
            
        }

}